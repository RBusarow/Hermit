name: Shartify

on:
  pull_request:
  workflow_dispatch:

env:
  macosGradleArgs: "-Dorg.gradle.jvmargs=-Xmx10g"
  ubuntuGradleArgs: "-Dorg.gradle.jvmargs=-Xmx5g"
  windowsGradleArgs: "-Dorg.gradle.jvmargs=-Xmx4g"

concurrency:
  group: ci-${{ github.ref }}-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  shard_0:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :compileJava
            :compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_1:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :detekt
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_2:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:compileJava
            :hermit-core:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_3:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:dependencyGuard
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_4:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_5:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_6:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_7:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_8:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:compileJava
            :hermit-core:samples:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_9:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_10:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_11:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_12:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_13:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:dependencyGuard
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_14:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_15:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_16:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_17:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_18:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:compileJava
            :hermit-coroutines:samples:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_19:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_20:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_21:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_22:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_23:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:dependencyGuard
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_24:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_25:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_26:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_27:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_28:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:compileJava
            :hermit-junit4:samples:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_29:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_30:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_31:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_32:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_33:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:dependencyGuard
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_34:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_35:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_36:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_37:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_38:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:compileJava
            :hermit-junit5:samples:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_39:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_40:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_41:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_42:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_43:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:dependencyGuard
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_44:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_45:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_46:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_47:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_48:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:compileJava
            :hermit-mockk:samples:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_49:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_50:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_51:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_52:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_53:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :lintKotlinMain
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_54:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :lintKotlinTest
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_55:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :moduleCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_56:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :processResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_57:
    runs-on: ubuntu-latest

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :processTestResources
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_58:
    runs-on: ubuntu-latest
    needs:
      - shard_4
      - shard_5

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_59:
    runs-on: ubuntu-latest
    needs:
      - shard_10
      - shard_9

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_60:
    runs-on: ubuntu-latest
    needs:
      - shard_15
      - shard_14

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_61:
    runs-on: ubuntu-latest
    needs:
      - shard_20
      - shard_19

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_62:
    runs-on: ubuntu-latest
    needs:
      - shard_25
      - shard_24

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_63:
    runs-on: ubuntu-latest
    needs:
      - shard_30
      - shard_29

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_64:
    runs-on: ubuntu-latest
    needs:
      - shard_35
      - shard_34

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_65:
    runs-on: ubuntu-latest
    needs:
      - shard_39
      - shard_40

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_66:
    runs-on: ubuntu-latest
    needs:
      - shard_45
      - shard_44

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_67:
    runs-on: ubuntu-latest
    needs:
      - shard_50
      - shard_49

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_68:
    runs-on: ubuntu-latest
    needs:
      - shard_54
      - shard_53

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :lintKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_69:
    runs-on: ubuntu-latest
    needs:
      - shard_0

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :apiBuild
            :apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_70:
    runs-on: ubuntu-latest
    needs:
      - shard_0
      - shard_56

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :classes
            :compileTestJava
            :compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_71:
    runs-on: ubuntu-latest
    needs:
      - shard_2

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:apiBuild
            :hermit-core:apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_72:
    runs-on: ubuntu-latest
    needs:
      - shard_2
      - shard_6

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_73:
    runs-on: ubuntu-latest
    needs:
      - shard_8
      - shard_11

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_74:
    runs-on: ubuntu-latest
    needs:
      - shard_18
      - shard_21

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_75:
    runs-on: ubuntu-latest
    needs:
      - shard_28
      - shard_31

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_76:
    runs-on: ubuntu-latest
    needs:
      - shard_38
      - shard_41

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_77:
    runs-on: ubuntu-latest
    needs:
      - shard_48
      - shard_51

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_78:
    runs-on: ubuntu-latest
    needs:
      - shard_72

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:compileTestJava
            :hermit-core:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_79:
    runs-on: ubuntu-latest
    needs:
      - shard_72

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:jar
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_80:
    runs-on: ubuntu-latest
    needs:
      - shard_73
      - shard_79

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:compileTestJava
            :hermit-core:samples:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_81:
    runs-on: ubuntu-latest
    needs:
      - shard_79

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:compileJava
            :hermit-coroutines:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_82:
    runs-on: ubuntu-latest
    needs:
      - shard_79

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:compileJava
            :hermit-junit4:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_83:
    runs-on: ubuntu-latest
    needs:
      - shard_79

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:compileJava
            :hermit-junit5:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_84:
    runs-on: ubuntu-latest
    needs:
      - shard_79

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:compileJava
            :hermit-mockk:compileKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_85:
    runs-on: ubuntu-latest
    needs:
      - shard_7
      - shard_78

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:test
            :hermit-core:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_86:
    runs-on: ubuntu-latest
    needs:
      - shard_57
      - shard_70

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :test
            :testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_87:
    runs-on: ubuntu-latest
    needs:
      - shard_12
      - shard_80

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:test
            :hermit-core:samples:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_88:
    runs-on: ubuntu-latest
    needs:
      - shard_81

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:apiBuild
            :hermit-coroutines:apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_89:
    runs-on: ubuntu-latest
    needs:
      - shard_81
      - shard_16

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_90:
    runs-on: ubuntu-latest
    needs:
      - shard_82

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:apiBuild
            :hermit-junit4:apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_91:
    runs-on: ubuntu-latest
    needs:
      - shard_82
      - shard_26

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_92:
    runs-on: ubuntu-latest
    needs:
      - shard_83

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:apiBuild
            :hermit-junit5:apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_93:
    runs-on: ubuntu-latest
    needs:
      - shard_83
      - shard_36

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:classes
            :hermit-junit5:transformAtomicfuClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_94:
    runs-on: ubuntu-latest
    needs:
      - shard_84

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:apiBuild
            :hermit-mockk:apiCheck
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_95:
    runs-on: ubuntu-latest
    needs:
      - shard_84
      - shard_46

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:classes
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_96:
    runs-on: ubuntu-latest
    needs:
      - shard_3
      - shard_71
      - shard_85
      - shard_58

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_97:
    runs-on: ubuntu-latest
    needs:
      - shard_89

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:jar
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_98:
    runs-on: ubuntu-latest
    needs:
      - shard_91

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:compileTestJava
            :hermit-junit4:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_99:
    runs-on: ubuntu-latest
    needs:
      - shard_91

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:jar
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_100:
    runs-on: ubuntu-latest
    needs:
      - shard_95

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:compileTestJava
            :hermit-mockk:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_101:
    runs-on: ubuntu-latest
    needs:
      - shard_95

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:jar
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_102:
    runs-on: ubuntu-latest
    needs:
      - shard_87
      - shard_59

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-core:samples:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_103:
    runs-on: ubuntu-latest
    needs:
      - shard_75
      - shard_99

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:compileTestJava
            :hermit-junit4:samples:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_104:
    runs-on: ubuntu-latest
    needs:
      - shard_93

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:compileTestJava
            :hermit-junit5:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_105:
    runs-on: ubuntu-latest
    needs:
      - shard_93

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:jar
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_106:
    runs-on: ubuntu-latest
    needs:
      - shard_89
      - shard_105

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:compileTestJava
            :hermit-coroutines:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_107:
    runs-on: ubuntu-latest
    needs:
      - shard_74
      - shard_97
      - shard_105

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:compileTestJava
            :hermit-coroutines:samples:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_108:
    runs-on: ubuntu-latest
    needs:
      - shard_27
      - shard_98

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:test
            :hermit-junit4:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_109:
    runs-on: ubuntu-latest
    needs:
      - shard_76
      - shard_105

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:compileTestJava
            :hermit-junit5:samples:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_110:
    runs-on: ubuntu-latest
    needs:
      - shard_77
      - shard_105
      - shard_101

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:compileTestJava
            :hermit-mockk:samples:compileTestKotlin
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_111:
    runs-on: ubuntu-latest
    needs:
      - shard_47
      - shard_100

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:test
            :hermit-mockk:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_112:
    runs-on: ubuntu-latest
    needs:
      - shard_32
      - shard_103

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:test
            :hermit-junit4:samples:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_113:
    runs-on: ubuntu-latest
    needs:
      - shard_37
      - shard_104

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:test
            :hermit-junit5:testClasses
            :hermit-junit5:transformTestAtomicfuClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_114:
    runs-on: ubuntu-latest
    needs:
      - shard_22
      - shard_107

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:test
            :hermit-coroutines:samples:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_115:
    runs-on: ubuntu-latest
    needs:
      - shard_17
      - shard_106

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:test
            :hermit-coroutines:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_116:
    runs-on: ubuntu-latest
    needs:
      - shard_108
      - shard_62
      - shard_90
      - shard_23

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_117:
    runs-on: ubuntu-latest
    needs:
      - shard_42
      - shard_109

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:test
            :hermit-junit5:samples:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_118:
    runs-on: ubuntu-latest
    needs:
      - shard_66
      - shard_43
      - shard_111
      - shard_94

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_119:
    runs-on: ubuntu-latest
    needs:
      - shard_52
      - shard_110

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:test
            :hermit-mockk:samples:testClasses
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_120:
    runs-on: ubuntu-latest
    needs:
      - shard_112
      - shard_63

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit4:samples:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_121:
    runs-on: ubuntu-latest
    needs:
      - shard_115
      - shard_64
      - shard_94
      - shard_85
      - shard_112
      - shard_58
      - shard_69
      - shard_86
      - shard_92
      - shard_88
      - shard_62
      - shard_87
      - shard_117
      - shard_108
      - shard_113
      - shard_13
      - shard_66
      - shard_33
      - shard_3
      - shard_90
      - shard_65
      - shard_71
      - shard_1
      - shard_55
      - shard_60
      - shard_23
      - shard_59
      - shard_68
      - shard_43
      - shard_114
      - shard_63
      - shard_61
      - shard_67
      - shard_119
      - shard_111

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_122:
    runs-on: ubuntu-latest
    needs:
      - shard_13
      - shard_88
      - shard_115
      - shard_60

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_123:
    runs-on: ubuntu-latest
    needs:
      - shard_61
      - shard_114

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-coroutines:samples:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_124:
    runs-on: ubuntu-latest
    needs:
      - shard_33
      - shard_113
      - shard_64
      - shard_92

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_125:
    runs-on: ubuntu-latest
    needs:
      - shard_65
      - shard_117

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-junit5:samples:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shard_126:
    runs-on: ubuntu-latest
    needs:
      - shard_119
      - shard_67

    steps:
      - name: check out with token
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "11"

      - name: the tasks
        uses: gradle/gradle-build-action@v2
        with:
          arguments: |
            :hermit-mockk:samples:check
            "${{ env.ubuntuGradleArgs }}"
          cache-read-only: false

  shartify-all-green:
    if: always()
    runs-on: ubuntu-latest
    needs:
      - shard_0
      - shard_1
      - shard_2
      - shard_3
      - shard_4
      - shard_5
      - shard_6
      - shard_7
      - shard_8
      - shard_9
      - shard_10
      - shard_11
      - shard_12
      - shard_13
      - shard_14
      - shard_15
      - shard_16
      - shard_17
      - shard_18
      - shard_19
      - shard_20
      - shard_21
      - shard_22
      - shard_23
      - shard_24
      - shard_25
      - shard_26
      - shard_27
      - shard_28
      - shard_29
      - shard_30
      - shard_31
      - shard_32
      - shard_33
      - shard_34
      - shard_35
      - shard_36
      - shard_37
      - shard_38
      - shard_39
      - shard_40
      - shard_41
      - shard_42
      - shard_43
      - shard_44
      - shard_45
      - shard_46
      - shard_47
      - shard_48
      - shard_49
      - shard_50
      - shard_51
      - shard_52
      - shard_53
      - shard_54
      - shard_55
      - shard_56
      - shard_57
      - shard_58
      - shard_59
      - shard_60
      - shard_61
      - shard_62
      - shard_63
      - shard_64
      - shard_65
      - shard_66
      - shard_67
      - shard_68
      - shard_69
      - shard_70
      - shard_71
      - shard_72
      - shard_73
      - shard_74
      - shard_75
      - shard_76
      - shard_77
      - shard_78
      - shard_79
      - shard_80
      - shard_81
      - shard_82
      - shard_83
      - shard_84
      - shard_85
      - shard_86
      - shard_87
      - shard_88
      - shard_89
      - shard_90
      - shard_91
      - shard_92
      - shard_93
      - shard_94
      - shard_95
      - shard_96
      - shard_97
      - shard_98
      - shard_99
      - shard_100
      - shard_101
      - shard_102
      - shard_103
      - shard_104
      - shard_105
      - shard_106
      - shard_107
      - shard_108
      - shard_109
      - shard_110
      - shard_111
      - shard_112
      - shard_113
      - shard_114
      - shard_115
      - shard_116
      - shard_117
      - shard_118
      - shard_119
      - shard_120
      - shard_121
      - shard_122
      - shard_123
      - shard_124
      - shard_125
      - shard_126

    steps:
      - name: require that all other jobs have passed
        uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}

